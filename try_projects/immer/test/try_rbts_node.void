{ v_import("mainline.void"); }
{ v_enable_mainline(); }
//---------------------------------------------------------------------
{
    v_import("cstdio.void");

    v_import("../memory_policy.void");

    v_import("../detail/rbts/node2.void");
}


//---------------------------------------------------------------------
{ v_import("stack_trace.void"); }
{ voidc_enable_stack_trace(); }


//---------------------------------------------------------------------
//my_type = struct { char[3]; };
//my_type = v_std_string_t;
my_type = int;
//my_type = char;

namespace my
{
    using namespace immer.detail.rbts;

    policy = default_memory_policy;

    BL = derive_bits_leaf(my_type, policy, 5);


//---------------------------------------------------------------------
}   //- namespace my


//{ voidc_verify_jit_module_optimized(true); }
//{ v_debug_print_module(2); }
{
    printf("BL: %d\n", my.BL);

    node_t = my.node(my_type, my.policy, 5, my.BL);

    printf("spln:  %zd\n", node_t.sizeof_packed_leaf_n(19));
    printf("spln_: %zd\n", node_t.sizeof_packed_leaf_n_(19));

    printf("spin:  %zd\n", node_t.sizeof_packed_inner_n(19));
    printf("spin_: %zd\n", node_t.sizeof_packed_inner_n_(19));

    printf("spir:  %zd\n", node_t.sizeof_packed_relaxed_n(19));
    printf("spir_: %zd\n", node_t.sizeof_packed_relaxed_n_(19));

    printf("spirn: %zd\n", node_t.sizeof_packed_inner_r_n(19));

    printf("msl: %zd\n", node_t.max_sizeof_leaf);
    printf("msi: %zd\n", node_t.max_sizeof_inner);
    printf("msr: %zd\n", node_t.max_sizeof_relaxed);
    printf("msir: %zd\n", node_t.max_sizeof_inner_r);



    heap = node_t.heap;

    p = (heap.allocate(sizeof(int[7])) : *int);

    printf("p: %p\n", p);

    printf("i: %zu\n", ((0: *node_t)->inner : size_t));
    printf("l: %zu\n", ((0: *node_t)->leaf : size_t));

    printf("r: %zu\n", (&(0: *node_t)->refs : size_t));


    heap.deallocate(sizeof(int[7]), p);


    n = 7;      printf("n: %d, %d\n", n, v_static_log2(n));
    n = 77;     printf("n: %d, %d\n", n, v_static_log2(n));
    n = 777;    printf("n: %d, %d\n", n, v_static_log2(n));
    n = 7777;   printf("n: %d, %d\n", n, v_static_log2(n));
    n = 77777;  printf("n: %d, %d\n", n, v_static_log2(n));





}


node2_t = my.node(long, my.policy, 5, my.derive_bits_leaf(long, my.policy, 5));

node3_t = my.node(my_type, my.policy, 5, my.BL);




